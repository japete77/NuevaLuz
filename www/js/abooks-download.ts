/// <reference path="app.ts" />

app.service('ABooksDownloader', ['$scope', '$ionicPlatform', '$cordovaFile', '$interval', '$rootScope', 
	function(BooksDownloader, $scope, $ionicPlatform, $cordovaFile, $interval, $rootScope) {
	
}]);